# TODO: Load environment variables via python-dotenv (OPENAI_API_KEY, etc.).
# TODO: Centralize configurable settings (model names, chunk size/overlap, vector_db path, top-k).
# TODO: Expose settings as a single config object/dataclass importable across rag/ and app.py.
